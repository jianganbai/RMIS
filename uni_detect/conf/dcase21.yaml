scale: sec  # mt2set, mt, sec
domsp: none
detector: ['knn']

knn_conf:
  n_neighbors: 1
  metric: cosine
  source:
    n_neighbors: 1
    metric: cosine
    normalize: True
  target:
    n_neighbors: 1
    metric: cosine
    normalize: True
